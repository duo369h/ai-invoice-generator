# Registry Audit

`docs/product-knowledge/registry.json` is a multi-entry registry: future knowledge packages append entries rather than replacing the registry. The initial entry points to:

`docs/product-knowledge/photography/quote-production-intelligence/v0.1/knowledge.json`

The entry records the knowledge ID, path, title, status, authority boundaries, primary consumers, tags, and source SHA-256.
