# Core regression

The full `npm test` suite passed, including revenue lock, first revenue loop, pricing, payment API/ledger/state, settled invoice write guards, portal payment guardrails, and dashboard entitlement runtime checks. DeepSeek was not called.
