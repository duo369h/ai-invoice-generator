# Database Migration

New migration: `20260903092326_r56b2_pro_combined_document_quota_authority.sql`, generated by Supabase CLI and applied to no database.

It uses `CREATE OR REPLACE FUNCTION` only for `get_user_active_document_cycle`; the historical migration is untouched. It preserves the existing account-anniversary fallback, uses active/trialing subscription `current_period_start/current_period_end` when valid for Starter or Pro, and returns finite limits for current plans. Agency and Studio legacy compatibility remains `NULL` cycle/limit.

Static validation passed. Production ref `fgortrxozlbzxbkerejz` was not contacted; the optional Sandbox was not required.
