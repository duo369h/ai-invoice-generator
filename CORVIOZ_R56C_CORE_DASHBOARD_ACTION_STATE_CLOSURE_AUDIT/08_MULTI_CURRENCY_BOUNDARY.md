# Multi-Currency Boundary

Currency groups are normalized to uppercase and aggregated independently. When more than one currency is present, top-level paid/outstanding totals are null; the UI renders each currency separately. Therefore no cross-currency sum is produced.
