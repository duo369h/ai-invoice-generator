# Internal route classification

`control-plane`, `evolution`, `optimization`, `revenue-validation`, `simulation`, `audit`, `validation`, and `product-funnel` are `INTERNAL_ONLY` / `EXPERIMENTAL` operational surfaces. `early-access` was classified as `INTERNAL_ONLY` before the boundary was applied. The core product route is `/dashboard`.
