# localStorage diagnostic authority

`corvioz_export_count` is a development-only diagnostic counter. `corvioz_usage_stats` is written only outside preview mode and only in development. Neither key drives quota, plan, entitlement, payment, or core workflow state.

LOCALSTORAGE_CORE_AUTHORITY=NO
