# Wave 1 CSS consolidation

The duplicate Wave 1 card block was removed and the canonical card, state, payment, document, and responsive rules were consolidated. The R56D test counts exactly one base `.dashboard-wave1-card` selector and `git diff --check` passes.

WAVE1_CSS_DUPLICATION=CLOSED
