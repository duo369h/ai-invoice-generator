diff --git a/src/app/api/proposals/generate/route.js b/src/app/api/proposals/generate/route.js
index 055fe3f..e2f0e26 100644
--- a/src/app/api/proposals/generate/route.js
+++ b/src/app/api/proposals/generate/route.js
@@ -58,6 +58,13 @@ function stripRevenueOutputFields(value) {
 }
 
 export async function POST(request) {
+  return NextResponse.json(
+    { error: 'This legacy document generator is unavailable.', code: 'LEGACY_SURFACE_UNAVAILABLE' },
+    { status: 410 }
+  );
+
+  // Historical implementation is retained below for source compatibility, but
+  // the current product never generates Proposal output through this route.
   try {
     const ip = getIp(request);
     const context = await getRequestUser(request);
diff --git a/src/app/dashboard/components/StudioSpace.js b/src/app/dashboard/components/StudioSpace.js
index ba42b83..d2b4d25 100644
--- a/src/app/dashboard/components/StudioSpace.js
+++ b/src/app/dashboard/components/StudioSpace.js
@@ -1401,7 +1401,7 @@ export default function StudioSpace({
             🏢 Small Business Dashboard
           </div>
           <h1 style={{ fontSize: '1.75rem', fontWeight: 800, margin: 0, letterSpacing: '-0.02em', display: 'flex', alignItems: 'center', gap: '12px', flexWrap: 'wrap' }}>
-            <span>Agency Operating System</span>
+            <span>Client workflow workspace</span>
             {businessModeBadge && (
               <span style={{
                 fontSize: '0.7rem',
diff --git a/src/app/proposal/page.js b/src/app/proposal/page.js
index 794ee15..bb39bb9 100644
--- a/src/app/proposal/page.js
+++ b/src/app/proposal/page.js
@@ -1,5 +1,5 @@
 import { redirect } from 'next/navigation';
 
 export default function ProposalPage() {
-  redirect('/dashboard?tool=proposal');
+  redirect('/dashboard?tool=quotes');
 }
diff --git a/src/app/proposals/page.js b/src/app/proposals/page.js
index 99b003f..ccd5193 100644
--- a/src/app/proposals/page.js
+++ b/src/app/proposals/page.js
@@ -5,5 +5,5 @@ export const revalidate = 0;
 export const fetchCache = 'force-no-store';
 
 export default function ProposalsPage() {
-  redirect('/dashboard?tool=proposal');
+  redirect('/dashboard?tool=quotes');
 }
diff --git a/src/app/styles/components.css b/src/app/styles/components.css
index f2ae9ca..4f0a340 100644
--- a/src/app/styles/components.css
+++ b/src/app/styles/components.css
@@ -742,7 +742,7 @@ html[data-theme="dark"] .blog-card-footer:hover .blog-card-cta {
 }
 
 @media (max-width: 430px) {
-  .dashboard-wave1-card {
+  .dashboard-wave1-overview > .dashboard-wave1-card {
     padding: 16px;
   }
 
@@ -767,7 +767,7 @@ html[data-theme="dark"] .blog-card-footer:hover .blog-card-cta {
     padding-right: 12px !important;
   }
 
-  .dashboard-wave1-card {
+  .dashboard-wave1-overview > .dashboard-wave1-card {
     padding: 14px;
   }
 
@@ -2083,349 +2083,6 @@ html[data-theme="dark"] .blog-card-footer:hover .blog-card-cta {
   background: var(--btn-secondary-bg);
 }
 
-.dashboard-wave1-overview {
-  display: flex;
-  flex-direction: column;
-  gap: var(--space-6);
-  width: 100%;
-  max-width: 1080px;
-  margin: 0 auto;
-}
-
-.dashboard-wave1-header {
-  display: flex;
-  flex-direction: column;
-  gap: var(--space-2);
-}
-
-.dashboard-wave1-header h1,
-.dashboard-wave1-header p {
-  margin: 0;
-}
-
-.dashboard-wave1-header h1 {
-  color: var(--text-main);
-  font-size: var(--text-3xl);
-  font-weight: 800;
-}
-
-.dashboard-wave1-header > p:last-child {
-  color: var(--text-muted);
-  font-size: var(--text-sm);
-}
-
-.dashboard-wave1-card {
-  padding: var(--space-6);
-  background: var(--bg-card);
-  border: 1px solid var(--border);
-  border-radius: var(--radius-lg);
-  box-shadow: var(--shadow-sm);
-}
-
-.dashboard-wave1-section-heading {
-  display: flex;
-  align-items: flex-start;
-  justify-content: space-between;
-  flex-wrap: wrap;
-  gap: var(--space-4);
-  margin-bottom: var(--space-5);
-}
-
-.dashboard-wave1-section-heading h2 {
-  margin: 0;
-  color: var(--text-main);
-  font-size: var(--text-xl);
-  font-weight: 750;
-}
-
-.dashboard-wave1-eyebrow,
-.dashboard-wave1-hint {
-  color: var(--text-muted);
-  font-size: var(--text-xs);
-}
-
-.dashboard-wave1-eyebrow {
-  margin: 0 0 var(--space-1);
-  color: var(--primary);
-  font-weight: 750;
-  letter-spacing: 0.04em;
-  text-transform: uppercase;
-}
-
-.dashboard-wave1-hint {
-  padding-top: var(--space-1);
-  white-space: nowrap;
-}
-
-.dashboard-wave1-actions {
-  display: grid;
-  grid-template-columns: repeat(2, minmax(0, 1fr));
-  gap: var(--space-3);
-}
-
-.dashboard-wave1-action {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  gap: var(--space-4);
-  min-height: 76px;
-  padding: var(--space-4) var(--space-5);
-  border: 1px solid var(--border);
-  border-radius: var(--radius-md);
-  background: var(--bg-surface);
-  color: var(--text-main);
-  cursor: pointer;
-  text-align: left;
-  transition: background var(--motion-normal), border-color var(--motion-normal), transform var(--motion-normal);
-}
-
-.dashboard-wave1-action:hover,
-.dashboard-wave1-action:focus-visible {
-  border-color: var(--primary);
-  background: var(--primary-glow);
-  transform: translateY(-1px);
-}
-
-.dashboard-wave1-action > span:first-child {
-  display: flex;
-  flex-direction: column;
-  gap: var(--space-1);
-  min-width: 0;
-}
-
-.dashboard-wave1-action strong {
-  font-size: var(--text-sm);
-  font-weight: 750;
-}
-
-.dashboard-wave1-action small {
-  color: var(--text-muted);
-  font-size: var(--text-xs);
-}
-
-.dashboard-wave1-document-list {
-  display: flex;
-  flex-direction: column;
-  gap: var(--space-3);
-}
-
-.dashboard-wave1-document {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  gap: var(--space-4);
-  min-width: 0;
-  padding: var(--space-4);
-  border: 1px solid var(--border);
-  border-radius: var(--radius-md);
-  background: var(--bg-surface);
-}
-
-.dashboard-wave1-document-main,
-.dashboard-wave1-document-meta {
-  display: flex;
-  min-width: 0;
-}
-
-.dashboard-wave1-document-main {
-  flex-direction: column;
-  gap: var(--space-1);
-}
-
-.dashboard-wave1-document-main h3,
-.dashboard-wave1-document-main p {
-  margin: 0;
-  overflow-wrap: anywhere;
-}
-
-.dashboard-wave1-document-main h3 {
-  color: var(--text-main);
-  font-size: var(--text-sm);
-  font-weight: 750;
-}
-
-.dashboard-wave1-document-main p {
-  color: var(--text-muted);
-  font-size: var(--text-xs);
-}
-
-.dashboard-wave1-type {
-  color: var(--text-muted);
-  font-size: var(--text-xs);
-  font-weight: 750;
-  letter-spacing: 0.04em;
-  text-transform: uppercase;
-}
-
-.dashboard-wave1-document-meta {
-  align-items: center;
-  justify-content: flex-end;
-  flex-wrap: wrap;
-  gap: var(--space-3);
-}
-
-.dashboard-wave1-document-meta > strong {
-  color: var(--text-main);
-  font-size: var(--text-sm);
-  white-space: nowrap;
-}
-
-.dashboard-wave1-status {
-  padding: 4px 8px;
-  border: 1px solid var(--border);
-  border-radius: var(--radius-pill);
-  background: var(--bg-card);
-  color: var(--text-muted);
-  font-size: var(--text-xs);
-  font-weight: 700;
-  white-space: nowrap;
-}
-
-.dashboard-wave1-status-paid,
-.dashboard-wave1-status-approved,
-.dashboard-wave1-status-converted {
-  border-color: var(--success);
-  background: var(--success-glow);
-  color: var(--success-text);
-}
-
-.dashboard-wave1-status-overdue,
-.dashboard-wave1-status-declined {
-  border-color: var(--danger);
-  background: var(--danger-glow);
-  color: var(--danger-text);
-}
-
-.dashboard-wave1-state {
-  display: flex;
-  flex-direction: column;
-  align-items: center;
-  gap: var(--space-2);
-  padding: var(--space-8) var(--space-4);
-  border: 1px dashed var(--border);
-  border-radius: var(--radius-md);
-  color: var(--text-main);
-  text-align: center;
-}
-
-.dashboard-wave1-state p {
-  max-width: 480px;
-  margin: 0;
-  color: var(--text-muted);
-  font-size: var(--text-sm);
-  line-height: 1.5;
-}
-
-.dashboard-wave1-spinner {
-  width: 28px;
-  height: 28px;
-  border: 3px solid var(--border);
-  border-top-color: var(--primary);
-  border-radius: 50%;
-  animation: spin 0.9s linear infinite;
-}
-
-.dashboard-wave1-empty-actions {
-  display: flex;
-  flex-wrap: wrap;
-  justify-content: center;
-  gap: var(--space-2);
-  margin-top: var(--space-2);
-}
-
-.dashboard-wave1-inline-error {
-  margin: 0 0 var(--space-3);
-  color: var(--danger-text);
-  font-size: var(--text-xs);
-}
-
-.dashboard-wave1-subtitle {
-  margin: 4px 0 0;
-  color: var(--text-muted);
-  font-size: var(--text-xs);
-  line-height: 1.4;
-}
-
-.dashboard-wave1-payment-metrics {
-  display: grid;
-  grid-template-columns: repeat(3, minmax(0, 1fr));
-  gap: var(--space-4);
-}
-
-.dashboard-wave1-payment-metrics > div,
-.dashboard-wave1-payment-group {
-  display: grid;
-  gap: var(--space-1);
-  min-width: 0;
-}
-
-.dashboard-wave1-payment-metrics span,
-.dashboard-wave1-payment-group span {
-  color: var(--text-muted);
-  font-size: var(--text-xs);
-}
-
-.dashboard-wave1-payment-metrics strong,
-.dashboard-wave1-payment-group strong {
-  color: var(--text-main);
-  font-size: var(--text-lg);
-  overflow-wrap: anywhere;
-}
-
-.dashboard-wave1-payment-groups {
-  display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
-  gap: var(--space-4);
-}
-
-.dashboard-wave1-payment-group {
-  padding: var(--space-3);
-  border: 1px solid var(--border);
-  border-radius: var(--radius-md);
-  background: var(--bg-surface);
-}
-
-.dashboard-wave1-usage-value {
-  display: block;
-  color: var(--text-main);
-  font-size: var(--text-2xl);
-  letter-spacing: -0.02em;
-}
-
-@media (max-width: 640px) {
-  .dashboard-wave1-card {
-    padding: var(--space-5);
-  }
-
-  .dashboard-wave1-actions {
-    grid-template-columns: 1fr;
-  }
-
-  .dashboard-wave1-payment-metrics {
-    grid-template-columns: 1fr;
-    gap: var(--space-3);
-  }
-
-  .dashboard-wave1-document {
-    align-items: flex-start;
-    flex-wrap: wrap;
-  }
-
-  .dashboard-wave1-document-meta {
-    width: 100%;
-    justify-content: flex-start;
-  }
-
-  .dashboard-needs-attention-item {
-    align-items: flex-start;
-    flex-direction: column;
-  }
-
-  .dashboard-needs-attention-main {
-    align-self: stretch;
-  }
-}
-
 .badge,
 .dashboard-status-badge {
   border-radius: var(--radius-pill);
@@ -4683,6 +4340,211 @@ html[data-theme="dark"] .blog-card-footer:hover .blog-card-cta {
   transform: translateX(2px);
 }
 
+.dashboard-wave1-action > span:first-child {
+  display: flex;
+  flex-direction: column;
+  gap: 4px;
+  min-width: 0;
+}
+
+.dashboard-wave1-document-list {
+  display: flex;
+  flex-direction: column;
+  gap: 12px;
+}
+
+.dashboard-wave1-document {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 16px;
+  min-width: 0;
+  padding: 14px 16px;
+  background: #fff;
+  border: 1px solid #e6e9ef;
+  border-radius: 12px;
+  box-shadow: none;
+  transition: border-color 170ms ease, background-color 170ms ease;
+}
+
+.dashboard-wave1-document:hover,
+.dashboard-wave1-document:focus-within {
+  background: #fbfcfe;
+  border-color: #d9d8e9;
+}
+
+.dashboard-wave1-document-main,
+.dashboard-wave1-document-meta {
+  display: flex;
+  min-width: 0;
+}
+
+.dashboard-wave1-document-main {
+  flex: 1 1 220px;
+  flex-direction: column;
+  gap: 4px;
+}
+
+.dashboard-wave1-document-main h3,
+.dashboard-wave1-document-main p {
+  margin: 0;
+  overflow-wrap: anywhere;
+}
+
+.dashboard-wave1-document-main h3 {
+  font-size: 0.95rem;
+  font-weight: 600;
+}
+
+.dashboard-wave1-document-main p {
+  font-weight: 400;
+}
+
+.dashboard-wave1-type {
+  color: var(--text-muted);
+  font-size: 0.75rem;
+  font-weight: 400;
+  letter-spacing: 0.04em;
+  text-transform: uppercase;
+}
+
+.dashboard-wave1-document-meta {
+  align-items: center;
+  justify-content: flex-end;
+  flex-wrap: wrap;
+  gap: 12px;
+}
+
+.dashboard-wave1-document-meta > strong {
+  max-width: 100%;
+  color: var(--text-main);
+  font-size: 0.875rem;
+  white-space: normal;
+  overflow-wrap: anywhere;
+}
+
+.dashboard-wave1-status {
+  padding: 4px 8px;
+  border: 1px solid var(--border);
+  border-radius: var(--radius-pill);
+  background: var(--bg-card);
+  color: var(--text-muted);
+  font-size: 0.72rem;
+  font-weight: 500;
+  white-space: nowrap;
+}
+
+.dashboard-wave1-status-paid,
+.dashboard-wave1-status-approved,
+.dashboard-wave1-status-converted {
+  border-color: var(--success);
+  background: var(--success-glow);
+  color: var(--success-text);
+}
+
+.dashboard-wave1-status-overdue,
+.dashboard-wave1-status-declined {
+  border-color: var(--danger);
+  background: var(--danger-glow);
+  color: var(--danger-text);
+}
+
+.dashboard-wave1-state {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 8px;
+  padding: 32px 16px;
+  border: 1px dashed var(--border);
+  border-radius: var(--radius-md);
+  color: var(--text-main);
+  text-align: center;
+}
+
+.dashboard-wave1-state p {
+  max-width: 480px;
+  margin: 0;
+  color: var(--text-muted);
+  font-size: 0.875rem;
+  line-height: 1.5;
+}
+
+.dashboard-wave1-spinner {
+  width: 28px;
+  height: 28px;
+  border: 3px solid var(--border);
+  border-top-color: var(--primary);
+  border-radius: 50%;
+  animation: spin 0.9s linear infinite;
+}
+
+.dashboard-wave1-empty-actions {
+  display: flex;
+  flex-wrap: wrap;
+  justify-content: center;
+  gap: 8px;
+  margin-top: 8px;
+}
+
+.dashboard-wave1-inline-error {
+  margin: 0 0 12px;
+  color: var(--danger-text);
+  font-size: 0.75rem;
+}
+
+.dashboard-wave1-subtitle {
+  margin: 4px 0 0;
+  color: var(--text-muted);
+  font-size: 0.75rem;
+  line-height: 1.4;
+}
+
+.dashboard-wave1-payment-metrics {
+  display: grid;
+  grid-template-columns: repeat(3, minmax(0, 1fr));
+  gap: 16px;
+}
+
+.dashboard-wave1-payment-metrics > div,
+.dashboard-wave1-payment-group {
+  display: grid;
+  gap: 4px;
+  min-width: 0;
+}
+
+.dashboard-wave1-payment-metrics span,
+.dashboard-wave1-payment-group span {
+  color: var(--text-muted);
+  font-size: 0.75rem;
+}
+
+.dashboard-wave1-payment-metrics strong,
+.dashboard-wave1-payment-group strong {
+  color: var(--text-main);
+  font-size: 1.125rem;
+  overflow-wrap: anywhere;
+}
+
+.dashboard-wave1-payment-groups {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
+  gap: 16px;
+}
+
+.dashboard-wave1-payment-group {
+  padding: 12px;
+  border: 1px solid var(--border);
+  border-radius: var(--radius-md);
+  background: var(--bg-surface);
+}
+
+.dashboard-wave1-usage-value {
+  display: block;
+  color: var(--text-main);
+  font-size: 1.5rem;
+  letter-spacing: -0.02em;
+}
+
 .dashboard-needs-attention-list {
   display: flex;
   flex-direction: column;
@@ -4879,36 +4741,6 @@ html[data-theme="dark"] .blog-card-footer:hover .blog-card-cta {
   font-size: 0.75rem;
 }
 
-.dashboard-wave1-document {
-  padding: 14px 16px;
-  background: #fff;
-  border: 1px solid #e6e9ef;
-  border-radius: 12px;
-  box-shadow: none;
-  transition: border-color 170ms ease, background-color 170ms ease;
-}
-
-.dashboard-wave1-document:hover,
-.dashboard-wave1-document:focus-within {
-  background: #fbfcfe;
-  border-color: #d9d8e9;
-}
-
-.dashboard-wave1-document-main h3 {
-  font-size: 0.95rem;
-  font-weight: 600;
-}
-
-.dashboard-wave1-document-main p,
-.dashboard-wave1-type {
-  font-weight: 400;
-}
-
-.dashboard-wave1-status {
-  font-size: 0.72rem;
-  font-weight: 500;
-}
-
 .dashboard-sidebar-footer .dashboard-sidebar-plan-row > span:last-child {
   font-weight: 600;
 }
@@ -4967,10 +4799,19 @@ html[data-theme="dark"] .blog-card-footer:hover .blog-card-cta {
     justify-content: space-between !important;
   }
 
-  .dashboard-wave1-card {
+  .dashboard-wave1-overview > .dashboard-wave1-card {
     padding: 18px;
   }
 
+  .dashboard-wave1-document-main {
+    flex-basis: 100%;
+  }
+
+  .dashboard-wave1-document-meta {
+    width: 100%;
+    justify-content: flex-start;
+  }
+
   .dashboard-wave1-section-heading {
     align-items: flex-start;
     flex-wrap: wrap;
@@ -4993,3 +4834,27 @@ html[data-theme="dark"] .blog-card-footer:hover .blog-card-cta {
     margin-right: 0;
   }
 }
+
+.dashboard-modal-overlay {
+  overflow-y: auto;
+  overscroll-behavior: contain;
+  padding: max(12px, env(safe-area-inset-top, 0px)) max(12px, env(safe-area-inset-right, 0px)) max(12px, env(safe-area-inset-bottom, 0px)) max(12px, env(safe-area-inset-left, 0px));
+}
+
+.dashboard-modal-card {
+  min-width: 0;
+  max-height: calc(100dvh - 40px);
+  overflow-y: auto;
+  overflow-wrap: anywhere;
+}
+
+@media (max-width: 480px) {
+  .dashboard-modal-card {
+    width: 100% !important;
+    padding: 20px !important;
+  }
+
+  .dashboard-modal-card .btn {
+    white-space: normal;
+  }
+}
diff --git a/src/components/dashboard/Dashboard.js b/src/components/dashboard/Dashboard.js
index c082bde..cf674c5 100644
--- a/src/components/dashboard/Dashboard.js
+++ b/src/components/dashboard/Dashboard.js
@@ -98,6 +98,7 @@ const generateMockId = () => 'mock-' + Date.now();
 const getMockDateString = () => new Date().toISOString();
 const DASHBOARD_SIDEBAR_STORAGE_KEY = 'corvioz_dashboard_sidebar_collapsed';
 const TERMINAL_QUOTE_STATUSES = new Set(['approved', 'declined', 'converted']);
+const isDevelopment = process.env.NODE_ENV === 'development';
 
 function ClientDocumentsPanel({
   client,
@@ -926,7 +927,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
   const [isDebugPanelVisible, setIsDebugPanelVisible] = useState(true);
 
   useEffect(() => {
-    if (typeof window !== 'undefined') {
+    if (isDevelopment && typeof window !== 'undefined') {
       window.__corvioz_on_debug_event = () => {
         setDebugEventsCount(c => c + 1);
       };
@@ -939,18 +940,18 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
   }, []);
 
   useEffect(() => {
-    if (typeof window !== 'undefined') {
+    if (isDevelopment && typeof window !== 'undefined') {
       const count = Number(window.localStorage.getItem('corvioz_export_count') || 0);
       setExportCount(count);
     }
   }, []);
 
   useEffect(() => {
-    if (showExportPurposeModal) console.log('[INSTRUMENTATION] modal_rendered: ExportPurposeModal');
+    if (isDevelopment && showExportPurposeModal) console.log('[INSTRUMENTATION] modal_rendered: ExportPurposeModal');
   }, [showExportPurposeModal]);
 
   useEffect(() => {
-    console.log('[INSTRUMENTATION] exportCount_updated:', exportCount);
+    if (isDevelopment) console.log('[INSTRUMENTATION] exportCount_updated:', exportCount);
   }, [exportCount]);
 
   useEffect(() => {
@@ -1887,7 +1888,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
   }, [initialTool, previewMode]);
 
   useEffect(() => {
-    if (previewMode) return;
+    if (previewMode || !isDevelopment) return;
     if (typeof window !== 'undefined') {
       window.localStorage.setItem('corvioz_usage_stats', JSON.stringify({
         invoicesCount: invoices.length,
@@ -3173,7 +3174,9 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
     setShowExportPurposeModal(false);
 
     const documentType = name.startsWith('quote_') ? 'quote' : 'invoice';
-    const currentCount = Number(window.localStorage.getItem('corvioz_export_count') || 0);
+    const currentCount = isDevelopment
+      ? Number(window.localStorage.getItem('corvioz_export_count') || 0)
+      : exportCount;
     const newCount = currentCount + 1;
 
     let clientContextId = null;
@@ -3211,7 +3214,9 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
     };
 
     await evaluateAction('export_pdf', async () => {
-      window.localStorage.setItem('corvioz_export_count', String(newCount));
+      if (isDevelopment) {
+        window.localStorage.setItem('corvioz_export_count', String(newCount));
+      }
       setExportCount(newCount);
       sendEvent('FIRST_ACTION_TAKEN', { action: 'export_pdf', documentType, export_count: newCount, source: 'export_purpose_modal' });
       const downloadWatermarkFree = hasCleanPdf;
@@ -4287,7 +4292,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
                 </svg>
                 <p style={{ fontSize: '1.05rem', marginBottom: '8px', color: 'var(--text-main)', fontWeight: 700 }}>Convert casual inquiries into structured client projects with a visual, kanban-style sales CRM.</p>
                 <p style={{ fontSize: '0.85rem', marginBottom: '20px', maxWidth: '440px', marginLeft: 'auto', marginRight: 'auto', lineHeight: 1.45 }}>
-                  Every project inquiry submitted through your public profile page lands here automatically. Track stages from negotiation to won and generate AI-driven estimate drafts.
+                  Every project inquiry submitted through your public profile page lands here automatically. Track stages from negotiation to won and prepare quote drafts.
                 </p>
                 <button onClick={() => handleDashboardTabChange('profile', 'leads_empty_state')} className="btn btn-primary btn-sm" style={{ fontWeight: 700 }}>
                   Set Up & Share Public Profile
@@ -4422,8 +4427,8 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
 
             {/* Lead CRM Details Modal */}
             {showLeadModal && selectedLead && (
-              <div style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(5, 5, 8, 0.85)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 1000, backdropFilter: 'blur(8px)' }}>
-                <div className="card animate-fade-in" style={{ maxWidth: '520px', width: '90%', padding: '32px', background: 'var(--bg-card)', border: '1px solid var(--border)' }}>
+              <div className="dashboard-modal-overlay" style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(5, 5, 8, 0.85)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 1000, backdropFilter: 'blur(8px)' }}>
+                <div className="card animate-fade-in dashboard-modal-card" style={{ maxWidth: '520px', width: '90%', padding: '32px', background: 'var(--bg-card)', border: '1px solid var(--border)' }}>
                   <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '24px' }}>
                     <h3 style={{ fontSize: '1.2rem', fontWeight: 800, margin: 0 }}>CRM Lead: {selectedLead.name}</h3>
                     <button onClick={() => { setShowLeadModal(false); setSelectedLead(null); }} style={{ fontSize: '1.5rem', color: 'var(--text-muted)', cursor: 'pointer', background: 'none', border: 'none' }}>&times;</button>
@@ -7357,7 +7362,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
             aria-modal="true"
             aria-labelledby="beta-feedback-title"
             onSubmit={submitBetaFeedback}
-            className="card"
+            className="card dashboard-modal-card"
             style={{
               width: 'min(100%, 520px)',
               padding: '28px',
@@ -7471,6 +7476,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
           aria-modal="true"
           aria-labelledby="record-payment-title"
           data-testid="record-payment-modal"
+          className="dashboard-modal-overlay"
           style={{
             position: 'fixed',
             top: 0,
@@ -7486,7 +7492,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
           }}
         >
           <form
-            className="card animate-fade-in"
+            className="card animate-fade-in dashboard-modal-card"
             onSubmit={(event) => {
               event.preventDefault();
               handleRecordPayment(recordPaymentInvoice);
@@ -7580,7 +7586,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
       />
 
       {showExportPurposeModal && (
-        <div role="dialog" style={{
+        <div role="dialog" className="dashboard-modal-overlay" style={{
           position: 'fixed',
           top: 0,
           left: 0,
@@ -7593,7 +7599,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
           zIndex: 1000,
           backdropFilter: 'blur(8px)'
         }}>
-          <div className="card animate-fade-in" style={{
+          <div className="card animate-fade-in dashboard-modal-card" style={{
             maxWidth: '460px',
             width: '95%',
             padding: '28px',
@@ -7687,7 +7693,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
 
       {/* 3. Business Mode Activated Modal (1st Client created) */}
       {showBusinessModeModal && (
-        <div style={{
+        <div className="dashboard-modal-overlay" style={{
           position: 'fixed',
           top: 0,
           left: 0,
@@ -7700,7 +7706,7 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
           zIndex: 1000,
           backdropFilter: 'blur(8px)'
         }}>
-          <div className="card animate-fade-in" style={{
+          <div className="card animate-fade-in dashboard-modal-card" style={{
             maxWidth: '460px',
             width: '95%',
             padding: '28px',
@@ -7813,8 +7819,8 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
       )}
 
       {suggestedActionDoc && (
-        <div style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(0,0,0,0.7)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 1000, backdropFilter: 'blur(5px)' }}>
-          <div className="card animate-fade-in" style={{ maxWidth: '500px', width: '90%', padding: '32px', background: 'var(--background-card)', border: '1px solid var(--border)', borderRadius: '12px', textAlign: 'center' }}>
+        <div className="dashboard-modal-overlay" style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(0,0,0,0.7)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 1000, backdropFilter: 'blur(5px)' }}>
+          <div className="card animate-fade-in dashboard-modal-card" style={{ maxWidth: '500px', width: '90%', padding: '32px', background: 'var(--background-card)', border: '1px solid var(--border)', borderRadius: '12px', textAlign: 'center' }}>
               <span className="product-state-mark">Done</span>
             <h3 style={{ fontSize: '1.25rem', fontWeight: 800, margin: '0 0 8px 0', color: 'var(--text-main)' }}>
               {suggestedActionDoc.type === 'invoice' ? 'Invoice Ready!' : 'Quote Ready!'}
@@ -7935,8 +7941,8 @@ export default function Dashboard({ mode = 'live', initialTool: routeInitialTool
 
       {/* Post Export Suggested Actions & Insights Modal */}
       {postExportDoc && (
-        <div style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(0,0,0,0.7)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 1000, backdropFilter: 'blur(5px)' }}>
-          <div className="card animate-fade-in" style={{ maxWidth: '500px', width: '90%', padding: '32px', background: 'var(--background-card)', border: '1px solid var(--border)', borderRadius: '12px' }}>
+        <div className="dashboard-modal-overlay" style={{ position: 'fixed', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: 'rgba(0,0,0,0.7)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 1000, backdropFilter: 'blur(5px)' }}>
+          <div className="card animate-fade-in dashboard-modal-card" style={{ maxWidth: '500px', width: '90%', padding: '32px', background: 'var(--background-card)', border: '1px solid var(--border)', borderRadius: '12px' }}>
             <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '20px' }}>
               <h3 style={{ fontSize: '1.2rem', fontWeight: 800, margin: 0 }}>Document Exported</h3>
               <button onClick={() => setPostExportDoc(null)} style={{ fontSize: '1.5rem', color: 'var(--text-muted)', cursor: 'pointer', background: 'none', border: 'none' }}>&times;</button>
diff --git a/src/components/dashboard/dashboardWave1.mjs b/src/components/dashboard/dashboardWave1.mjs
index 5f45e3d..06e997f 100644
--- a/src/components/dashboard/dashboardWave1.mjs
+++ b/src/components/dashboard/dashboardWave1.mjs
@@ -166,6 +166,10 @@ function documentStatus(record, type) {
   return value === undefined || value === null || value === '' ? null : String(value);
 }
 
+function documentTieBreaker(document) {
+  return `${document.type}:${document.id || ''}:${document.number || ''}`;
+}
+
 const NEEDS_ATTENTION_PRIORITY = Object.freeze({
   past_due: 0,
   partial: 1,
@@ -325,6 +329,7 @@ export function buildRecentDocuments({ quotes = [], invoices = [] } = {}, limit
     currency: record?.currency || 'USD',
     status: documentStatus(record, 'quote'),
     timestamp: timestampFor(record),
+    tieBreaker: null,
     source: record,
   }));
   const invoiceDocuments = (Array.isArray(invoices) ? invoices : []).map((record, index) => ({
@@ -336,11 +341,18 @@ export function buildRecentDocuments({ quotes = [], invoices = [] } = {}, limit
     currency: record?.currency || 'USD',
     status: documentStatus(record, 'invoice'),
     timestamp: timestampFor(record),
+    tieBreaker: null,
     source: record,
   }));
 
-  return [...quoteDocuments, ...invoiceDocuments]
-    .sort((left, right) => right.timestamp - left.timestamp)
+  const documents = [...quoteDocuments, ...invoiceDocuments].map((document) => ({
+    ...document,
+    tieBreaker: documentTieBreaker(document),
+  }));
+
+  return documents
+    .sort((left, right) => right.timestamp - left.timestamp || left.tieBreaker.localeCompare(right.tieBreaker))
+    .map(({ tieBreaker, ...document }) => document)
     .slice(0, Math.max(0, limit));
 }
 
diff --git a/src/middleware.js b/src/middleware.js
new file mode 100644
index 0000000..98392d0
--- /dev/null
+++ b/src/middleware.js
@@ -0,0 +1,39 @@
+import { NextResponse } from 'next/server';
+
+const INTERNAL_DASHBOARD_PATHS = Object.freeze([
+  '/dashboard/control-plane',
+  '/dashboard/evolution',
+  '/dashboard/optimization',
+  '/dashboard/revenue-validation',
+  '/dashboard/simulation',
+  '/dashboard/audit',
+  '/dashboard/validation',
+  '/dashboard/product-funnel',
+  '/dashboard/early-access',
+]);
+
+export function isInternalDashboardPath(pathname = '') {
+  return INTERNAL_DASHBOARD_PATHS.some((route) => pathname === route || pathname.startsWith(`${route}/`));
+}
+
+export function middleware(request) {
+  if (process.env.NODE_ENV === 'production' && isInternalDashboardPath(request.nextUrl.pathname)) {
+    return NextResponse.redirect(new URL('/dashboard', request.url));
+  }
+
+  return NextResponse.next();
+}
+
+export const config = {
+  matcher: [
+    '/dashboard/control-plane/:path*',
+    '/dashboard/evolution/:path*',
+    '/dashboard/optimization/:path*',
+    '/dashboard/revenue-validation/:path*',
+    '/dashboard/simulation/:path*',
+    '/dashboard/audit/:path*',
+    '/dashboard/validation/:path*',
+    '/dashboard/product-funnel/:path*',
+    '/dashboard/early-access/:path*',
+  ],
+};
diff --git a/scripts/test-r56d-dashboard-ux-closure.mjs b/scripts/test-r56d-dashboard-ux-closure.mjs
new file mode 100644
index 0000000..8cd2501
--- /dev/null
+++ b/scripts/test-r56d-dashboard-ux-closure.mjs
@@ -0,0 +1,91 @@
+import assert from 'node:assert/strict';
+import fs from 'node:fs';
+import { buildRecentDocuments } from '../src/components/dashboard/dashboardWave1.mjs';
+
+const read = (file) => fs.readFileSync(file, 'utf8');
+const root = new URL('../', import.meta.url).pathname;
+const source = (file) => {
+  const path = `${root}${file}`;
+  return fs.existsSync(path) ? read(path) : '';
+};
+
+const middleware = source('src/middleware.js');
+const proposalPage = source('src/app/proposal/page.js');
+const proposalsPage = source('src/app/proposals/page.js');
+const proposalApi = source('src/app/api/proposals/generate/route.js');
+const dashboard = source('src/components/dashboard/Dashboard.js');
+const debugOverlay = source('src/components/DevDebugOverlay.js');
+const styles = source('src/app/styles/components.css');
+
+const internalRoutes = [
+  'control-plane', 'evolution', 'optimization', 'revenue-validation',
+  'simulation', 'audit', 'validation', 'product-funnel', 'early-access',
+];
+
+assert.match(middleware, /process\.env\.NODE_ENV\s*===\s*['"]production['"]/);
+for (const route of internalRoutes) {
+  assert.match(middleware, new RegExp(`/dashboard/${route}`), `${route} must be covered by the production boundary`);
+}
+assert.match(middleware, /NextResponse\.redirect/);
+console.log('INTERNAL_EXPERIMENTAL_PRODUCTION_BOUNDARY=PASS');
+
+assert.match(proposalPage, /redirect\(['"]\/dashboard\?tool=quotes['"]\)/);
+assert.match(proposalsPage, /redirect\(['"]\/dashboard\?tool=quotes['"]\)/);
+assert.doesNotMatch(proposalPage, /tool=proposal/);
+assert.doesNotMatch(proposalsPage, /tool=proposal/);
+assert.match(proposalApi, /LEGACY_SURFACE_UNAVAILABLE/);
+assert.match(proposalApi, /return\s+NextResponse\.json/);
+console.log('PROPOSAL_COMPATIBILITY_BOUNDARY=PASS');
+
+assert.doesNotMatch(dashboard, /Copy (?:Client )?Portal Link|sandbox Portal URL/i);
+assert.doesNotMatch(dashboard, /Portal is available/i);
+console.log('PORTAL_SANDBOX_REMNANTS=PASS');
+
+assert.match(dashboard, /process\.env\.NODE_ENV\s*===\s*['"]development['"]\s*&&\s*\(/);
+assert.match(debugOverlay, /process\.env\.NODE_ENV\s*===\s*['"]development['"]/);
+console.log('PRODUCTION_DEBUG_UI_RENDERED=NO');
+
+assert.match(dashboard, /isDevelopment/);
+assert.match(dashboard, /corvioz_export_count/);
+assert.match(dashboard, /corvioz_usage_stats/);
+assert.match(dashboard, /isDevelopment\s*&&[\s\S]{0,240}corvioz_export_count/);
+assert.match(dashboard, /previewMode\s*\|\|\s*!isDevelopment/);
+console.log('LOCALSTORAGE_CORE_AUTHORITY=NO');
+
+const wave1CardCount = (styles.match(/^\.dashboard-wave1-card\s*\{/gm) || []).length;
+assert.equal(wave1CardCount, 1, 'Wave 1 card must have one canonical selector block');
+assert.match(styles, /\.dashboard-wave1-document-main\s*,[\s\S]*?flex:\s*1\s+1\s+220px/);
+assert.match(styles, /\.dashboard-wave1-document-meta\s*>\s*strong[\s\S]*?white-space:\s*normal/);
+console.log('WAVE1_CSS_DUPLICATION=CLOSED');
+
+const identicalTimestampFixture = {
+  quotes: [
+    { id: 'q-z', quote_number: 'QT-Z', status: 'sent', updated_at: '2026-09-03T10:00:00Z' },
+    { id: 'q-a', quote_number: 'QT-A', status: 'sent', updated_at: '2026-09-03T10:00:00Z' },
+  ],
+  invoices: [
+    { id: 'i-z', invoice_number: 'INV-Z', status: 'sent', updated_at: '2026-09-03T10:00:00Z' },
+    { id: 'i-a', invoice_number: 'INV-A', status: 'sent', updated_at: '2026-09-03T10:00:00Z' },
+  ],
+};
+const firstOrder = buildRecentDocuments(identicalTimestampFixture).map(({ type, id }) => `${type}:${id}`);
+const secondOrder = buildRecentDocuments({
+  quotes: identicalTimestampFixture.quotes.slice().reverse(),
+  invoices: identicalTimestampFixture.invoices.slice().reverse(),
+}).map(({ type, id }) => `${type}:${id}`);
+assert.deepEqual(firstOrder, secondOrder);
+assert.deepEqual(firstOrder, ['invoice:i-a', 'invoice:i-z', 'quote:q-a', 'quote:q-z']);
+console.log('RECENT_DOCUMENT_EQUAL_TIMESTAMP_DETERMINISM=PASS');
+
+assert.match(dashboard, /dashboard-modal-card/);
+assert.match(styles, /\.dashboard-modal-overlay[\s\S]*?overflow-y:\s*auto/);
+assert.match(styles, /\.dashboard-modal-card[\s\S]*?max-height:\s*calc\(100dvh\s*-\s*40px\)/);
+assert.match(styles, /overflow-wrap:\s*anywhere/);
+console.log('MOBILE_CORE_ACCEPTANCE=PASS');
+console.log('MODAL_LAYERING=PASS');
+
+assert.doesNotMatch(dashboard, /AI-driven estimate drafts/);
+assert.doesNotMatch(source('src/app/dashboard/components/StudioSpace.js'), /Agency Operating System/);
+console.log('EMPTY_ERROR_LANGUAGE=PASS');
+
+console.log('R56D DASHBOARD UX CLOSURE TEST=PASS');
