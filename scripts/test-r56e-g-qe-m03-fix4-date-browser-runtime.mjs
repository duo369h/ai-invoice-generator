import assert from 'node:assert/strict';
import http from 'node:http';
import net from 'node:net';
import fs from 'node:fs';
import path from 'node:path';
import { createRequire } from 'node:module';
import { spawn } from 'node:child_process';

const cwd = process.cwd();
const requireFromProject = createRequire(path.join(cwd, 'package.json'));
const { chromium } = requireFromProject('playwright');
const nextCli = requireFromProject.resolve('next/dist/bin/next');
const outputDir = path.join(cwd, 'output/playwright/r56e-g-qe-m03-fix4');
fs.mkdirSync(outputDir, { recursive: true });
const TEST_USER = { id: '55555555-5555-4555-8555-555555555555', aud: 'authenticated', role: 'authenticated', email: 'photographer@example.com', app_metadata: { provider: 'email' }, user_metadata: {} };
const base64Url = (value) => Buffer.from(JSON.stringify(value)).toString('base64url');
const createSession = () => { const expiresAt = Math.floor(Date.now() / 1000) + 3600; return { access_token: [base64Url({ alg: 'none', typ: 'JWT' }), base64Url({ ...TEST_USER, exp: expiresAt }), 'browser-test-signature'].join('.'), refresh_token: 'browser-test-refresh-token', expires_in: 3600, expires_at: expiresAt, token_type: 'bearer', user: TEST_USER }; };
const getFreePort = () => new Promise((resolve, reject) => { const server = net.createServer(); server.once('error', reject); server.listen(0, '127.0.0.1', () => { const port = server.address().port; server.close((error) => (error ? reject(error) : resolve(port))); }); });
const sendJson = (response, status, body) => { response.writeHead(status, { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' }); response.end(JSON.stringify(body)); };
async function startMockSupabase() { const server = http.createServer((request, response) => { const requestUrl = new URL(request.url, 'http://127.0.0.1'); if (request.method === 'OPTIONS') { response.writeHead(204, { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': 'authorization, apikey, content-type, x-client-info, x-supabase-api-version', 'Access-Control-Allow-Methods': 'GET, POST, PUT, PATCH, DELETE, OPTIONS' }); response.end(); return; } if (requestUrl.pathname === '/auth/v1/user') return sendJson(response, 200, TEST_USER); if (requestUrl.pathname === '/auth/v1/token') return sendJson(response, 200, createSession()); if (requestUrl.pathname === '/auth/v1/logout') { response.writeHead(204, { 'Access-Control-Allow-Origin': '*' }); response.end(); return; } if (requestUrl.pathname === '/rest/v1/entitlements') return sendJson(response, 200, [{ user_id: TEST_USER.id, invoice: true, export_pdf: false, client_portal: false, crm: false, automation: false, advanced_invoicing: false }]); return sendJson(response, 200, []); }); const port = await getFreePort(); await new Promise((resolve, reject) => { server.once('error', reject); server.listen(port, '127.0.0.1', resolve); }); return { url: `http://127.0.0.1:${port}`, close: () => new Promise((resolve) => server.close(() => resolve())) }; }
async function startNext(mockSupabaseUrl) { const port = await getFreePort(); const baseUrl = `http://127.0.0.1:${port}`; const child = spawn(process.execPath, [nextCli, 'dev', '--webpack', '--hostname', '127.0.0.1', '--port', String(port)], { cwd, env: { ...process.env, NEXT_PUBLIC_SUPABASE_URL: mockSupabaseUrl, NEXT_PUBLIC_SUPABASE_ANON_KEY: 'browser-test-anon-key' }, stdio: ['ignore', 'pipe', 'pipe'] }); let output = ''; child.stdout.on('data', (chunk) => { output += chunk.toString(); }); child.stderr.on('data', (chunk) => { output += chunk.toString(); }); const deadline = Date.now() + 60_000; while (Date.now() < deadline) { if (child.exitCode !== null) throw new Error(`Next server exited early: ${output}`); try { if ((await fetch(`${baseUrl}/auth`)).ok) return { baseUrl, close: () => new Promise((resolve) => { child.once('exit', resolve); child.kill('SIGTERM'); }) }; } catch (_) {} await new Promise((resolve) => setTimeout(resolve, 250)); } child.kill('SIGTERM'); throw new Error(`Timed out waiting for Next server: ${output}`); }
const scope = { version: 2, common: { shoot_type: 'Headshot', shoot_date: '2026-10-10', shoot_duration: 90, primary_location: 'Studio', coverage_expectation: 'Two looks', deliverables: ['Edited gallery'], final_image_count: 20, retouched_image_count: 10, delivery_format: ['JPEG'], delivery_deadline: '2026-10-20', exclusions: [], assumptions: [], usage_rights: { status: 'unspecified', purpose: null, media_channels: [], territory: null, license_duration: null, exclusivity: null } } };
const notes = `Existing notes\n\n---METADATA---\n${JSON.stringify({ quote_preset_id: 'portrait-session', photography_scope_v2: scope })}`;
function apiBody(pathname) { if (pathname === '/api/user') return { id: TEST_USER.id, email: TEST_USER.email, name: 'Avery Photographer', plan: 'free', hasActivated: true, auth_mode: 'supabase', quota: {} }; if (pathname === '/api/quotes') return { data: [{ id: 'quote-m03-fix4', quote_number: 'QT-M03-FIX4', client_name: 'Hydrated Client', client_email: 'hydrated@example.com', client_address: '400 Scope Street', currency: 'USD', total: 10000, status: 'draft', notes, items: [{ description: 'Portrait session', quantity: 1, unit_price: 10000 }] }] }; if (pathname === '/api/clients') return { data: [{ id: 'client-m03-fix4', name: 'Existing Client', email: 'existing@example.com', address: '500 Client Lane' }] }; if (pathname === '/api/invoices' || pathname === '/api/leads') return { data: [] }; if (pathname === '/api/card-profile') return { data: { id: 'profile-m03-fix4' } }; return { data: [] }; }
async function createPage(browser, baseUrl, viewport, locale) { const context = await browser.newContext({ viewport, locale }); const session = createSession(); await context.addInitScript((stored) => { localStorage.setItem('corvioz_analytics_consent', 'accepted'); localStorage.setItem('sb-127-auth-token', JSON.stringify(stored)); }, session); await context.addCookies([{ name: 'sb-127-auth-token.0', value: encodeURIComponent(JSON.stringify(session)), url: baseUrl }, { name: 'corvioz_analytics_consent', value: 'accepted', url: baseUrl }]); const page = await context.newPage(); const requests = []; const pageErrors = []; page.on('request', (request) => requests.push({ method: request.method(), url: request.url() })); page.on('pageerror', (error) => pageErrors.push(error.message)); await page.route('**/api/**', async (route) => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify(apiBody(new URL(route.request().url()).pathname)) })); return { context, page, requests, pageErrors }; }
async function hideDebug(page) { await page.evaluate(() => { const audit = [...document.querySelectorAll('body *')].find((node) => node.textContent?.trim() === 'Corvioz Verification Audit'); if (audit?.parentElement?.parentElement?.parentElement) audit.parentElement.parentElement.parentElement.style.display = 'none'; const debug = [...document.querySelectorAll('button')].find((node) => /Kernel Dev Debug|Debug UI/.test(node.textContent || '')); if (debug?.parentElement) debug.parentElement.style.display = 'none'; }); }
async function openEditor(page, baseUrl) { await page.goto(`${baseUrl}/dashboard?tool=quotes`, { waitUntil: 'domcontentloaded' }); await page.getByRole('heading', { name: 'Quotes', exact: true }).waitFor({ state: 'visible', timeout: 20_000 }); await page.getByRole('button', { name: 'Create Quote', exact: true }).click(); await page.locator('[data-quote-presentation-mode]').waitFor({ state: 'attached' }); await page.waitForFunction(() => document.querySelector('[data-quote-presentation-mode]')?.dataset.quotePresentationMode !== 'unresolved'); await hideDebug(page); }
async function selectWorkflow(page, name) { if (['Commercial', 'Portrait', 'Event'].includes(name)) { await page.getByRole('button', { name, exact: true }).click(); return; } await page.getByText('More workflows', { exact: true }).click(); await page.getByRole('button', { name, exact: true }).click(); }
async function goToScope(page, name) { await selectWorkflow(page, name); await page.getByTestId('quote-guided-shell').getByRole('button', { name: 'Continue', exact: true }).click(); await page.getByTestId('quote-guided-client-name').fill('M03 Fix4 Client'); await page.getByTestId('quote-guided-client-email').fill('fix4@example.com'); await page.getByTestId('quote-guided-client-step').getByRole('button', { name: 'Continue', exact: true }).click(); await page.getByTestId('quote-guided-scope-step').waitFor({ state: 'visible' }); }
async function ensureShootOpen(page) { if (await page.locator('[data-scope-edit-block="SHOOT"]:visible').count() === 0) await page.getByRole('button', { name: /Shoot details/ }).click(); }
const isDomainMutation = ({ method, url }) => ['POST', 'PUT', 'PATCH', 'DELETE'].includes(method) && !/(?:\/analytics(?:_events)?|\/revenue\/control-plane)(?:$|\/|\?)/.test(url);
const evidence = [];
async function assertDateControl(page, field, label) { const locator = page.getByTestId(`quote-guided-scope-${field}`); assert.equal(await locator.getAttribute('type'), 'text', `${label}: text type`); assert.equal(await locator.getAttribute('placeholder'), 'YYYY-MM-DD', `${label}: placeholder`); assert.equal(await locator.getAttribute('inputmode'), 'numeric', `${label}: numeric input mode`); assert.equal(await locator.getAttribute('pattern'), '\\d{4}-\\d{2}-\\d{2}', `${label}: pattern`); assert.equal(await locator.getAttribute('maxlength'), '10', `${label}: maxLength`); assert.equal(await locator.getAttribute('data-guided-date-entry'), 'english', `${label}: English entry marker`); assert.equal(await page.locator('[data-scope-edit-block]:visible input[type="date"]').count(), 0, `${label}: native date UI absent`); evidence.push({ label, field, type: await locator.getAttribute('type'), placeholder: await locator.getAttribute('placeholder'), inputMode: await locator.getAttribute('inputmode'), pattern: await locator.getAttribute('pattern'), maxLength: await locator.getAttribute('maxlength') }); return locator; }
async function editDate(page, field, value, label) { const locator = page.getByTestId(`quote-guided-scope-${field}`); await locator.fill(value); await locator.press('Tab'); await page.waitForTimeout(50); evidence.push({ label, field, entered: value, reflected: await locator.inputValue() }); return locator; }
async function runWorkflowDate(page, name, label, delivery = false) { await openEditor(page, server.baseUrl); await goToScope(page, name); await page.getByRole('button', { name: /Shoot details/ }).click(); await assertDateControl(page, 'shoot_date', label); await editDate(page, 'shoot_date', '2026-10-10', `${label} valid date`); assert.equal(await page.getByTestId('quote-guided-scope-shoot_date').inputValue(), '2026-10-10', `${label}: reflected date`); assert.equal(await page.getByRole('heading', { name: 'Define the shoot', exact: true }).textContent(), 'Define the shoot', `${label}: English heading`); assert.ok(await page.getByText('Continue', { exact: true }).count() > 0, `${label}: English Continue copy`); if (delivery) { await page.getByRole('button', { name: /Deliverables/ }).click(); await assertDateControl(page, 'delivery_deadline', `${label} delivery deadline`); await editDate(page, 'delivery_deadline', '2026-10-20', `${label} delivery deadline edit`); assert.equal(await page.getByTestId('quote-guided-scope-delivery_deadline').inputValue(), '2026-10-20', `${label}: reflected delivery deadline`); } }

const supabase = await startMockSupabase(); const server = await startNext(supabase.url); const browser = await chromium.launch({ headless: true });
try {
  const portraitEn = await createPage(browser, server.baseUrl, { width: 390, height: 844 }, 'en-US');
  try { await runWorkflowDate(portraitEn.page, 'Portrait', 'Portrait en-US'); await editDate(portraitEn.page, 'shoot_date', '2026-02-30', 'Portrait invalid date'); assert.equal(await portraitEn.page.getByTestId('quote-guided-scope-shoot_date').inputValue(), '', 'Invalid calendar date normalized to empty'); await editDate(portraitEn.page, 'shoot_date', '2026-10-10', 'Portrait restored date'); await portraitEn.page.screenshot({ path: path.join(outputDir, '390-portrait-date-expanded-en-US.png'), fullPage: true }); await portraitEn.page.getByTestId('quote-guided-scope-step').getByRole('button', { name: 'Back', exact: true }).click(); await portraitEn.page.getByTestId('quote-guided-client-step').getByRole('button', { name: 'Continue', exact: true }).click(); await portraitEn.page.getByTestId('quote-guided-scope-step').waitFor({ state: 'visible' }); await ensureShootOpen(portraitEn.page); assert.equal(await portraitEn.page.getByTestId('quote-guided-scope-shoot_date').inputValue(), '2026-10-10', 'Portrait date survives navigation'); await portraitEn.page.setViewportSize({ width: 1023, height: 844 }); await portraitEn.page.waitForFunction(() => document.querySelector('[data-quote-presentation-mode]')?.dataset.quotePresentationMode === 'guided'); assert.equal(await portraitEn.page.getByTestId('quote-guided-scope-shoot_date').inputValue(), '2026-10-10', '1023 Guided date continuity'); await portraitEn.page.setViewportSize({ width: 1024, height: 844 }); await portraitEn.page.waitForFunction(() => document.querySelector('[data-quote-presentation-mode]')?.dataset.quotePresentationMode === 'desktop'); await portraitEn.page.locator('#quote-scope-shoot_date').waitFor({ state: 'visible' }); assert.equal(await portraitEn.page.locator('#quote-scope-shoot_date').inputValue(), '2026-10-10', '1024 Desktop date continuity'); await portraitEn.page.setViewportSize({ width: 390, height: 844 }); await portraitEn.page.waitForFunction(() => document.querySelector('[data-quote-presentation-mode]')?.dataset.quotePresentationMode === 'guided'); await portraitEn.page.getByTestId('quote-guided-shell').getByRole('button', { name: 'Continue', exact: true }).click(); await portraitEn.page.getByTestId('quote-guided-client-step').getByRole('button', { name: 'Continue', exact: true }).click(); await portraitEn.page.getByTestId('quote-guided-scope-step').waitFor({ state: 'visible' }); await ensureShootOpen(portraitEn.page); assert.equal(await portraitEn.page.getByTestId('quote-guided-scope-shoot_date').inputValue(), '2026-10-10', '390 Guided date continuity'); assert.equal(portraitEn.pageErrors.length, 0, 'Portrait en-US page errors'); assert.deepEqual(portraitEn.requests.filter(isDomainMutation), [], 'Portrait en-US no server mutation'); } finally { await portraitEn.context.close(); }
  const portraitZh = await createPage(browser, server.baseUrl, { width: 390, height: 844 }, 'zh-CN'); try { await runWorkflowDate(portraitZh.page, 'Portrait', 'Portrait zh-CN'); assert.equal(await portraitZh.page.getByText('Scope', { exact: true }).count() > 0, true, 'zh-CN Scope copy remains English'); assert.equal(await portraitZh.page.getByText('Shoot date', { exact: true }).count() > 0, true, 'zh-CN Shoot date copy remains English'); assert.equal(await portraitZh.page.locator('input[placeholder="mm/dd/yyyy"]').count(), 0, 'zh-CN mm/dd/yyyy placeholder absent'); await editDate(portraitZh.page, 'shoot_date', '2026-10-10', 'Portrait zh-CN date'); await portraitZh.page.screenshot({ path: path.join(outputDir, '390-portrait-date-expanded-zh-CN.png'), fullPage: true }); assert.equal(portraitZh.pageErrors.length, 0, 'Portrait zh-CN page errors'); assert.deepEqual(portraitZh.requests.filter(isDomainMutation), [], 'Portrait zh-CN no server mutation'); } finally { await portraitZh.context.close(); }
  const wedding = await createPage(browser, server.baseUrl, { width: 390, height: 844 }, 'en-US'); try { await runWorkflowDate(wedding.page, 'Wedding', 'Wedding en-US'); assert.equal(await wedding.page.getByTestId('quote-guided-scope-shoot_date').inputValue(), '2026-10-10', 'Wedding date edit'); assert.equal(wedding.pageErrors.length, 0, 'Wedding page errors'); assert.deepEqual(wedding.requests.filter(isDomainMutation), [], 'Wedding no server mutation'); } finally { await wedding.context.close(); }
  const commercial = await createPage(browser, server.baseUrl, { width: 390, height: 844 }, 'en-US'); try { await runWorkflowDate(commercial.page, 'Commercial', 'Commercial en-US', true); assert.equal(commercial.pageErrors.length, 0, 'Commercial page errors'); assert.deepEqual(commercial.requests.filter(isDomainMutation), [], 'Commercial no server mutation'); } finally { await commercial.context.close(); }
} finally { await browser.close(); await server.close(); await supabase.close(); }
fs.writeFileSync(path.join(outputDir, 'date-browser-evidence.json'), JSON.stringify({ deterministicScopeDate: 'PASS', enUsDatePresentation: 'YYYY-MM-DD', zhCnDatePresentation: 'YYYY-MM-DD', zhCnPrimaryUiEnglish: 'PASS', nativeDateUi: 'ABSENT', invalidDateNormalization: 'PASS', dateCrossBreakpointContinuity: 'PASS', scopeInputRuntimeErrors: 0, guidedServerMutations: 0, evidence }, null, 2));
console.log(`R56E-G-QE-M03-FIX-4 deterministic date browser runtime: PASS (${outputDir})`);
