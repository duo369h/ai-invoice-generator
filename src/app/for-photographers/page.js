import ForPhotographersV1 from './ForPhotographersV1';

export const metadata = {
  title: 'Corvioz for photographers',
  description: 'A focused photography workflow for clear scope, client approval, invoices, and payment records.',
  alternates: { canonical: '/for-photographers' },
};

export default function ForPhotographersPage() {
  return <ForPhotographersV1 />;
}
